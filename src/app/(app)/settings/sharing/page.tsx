import { PageShell, PhaseStub } from "@/components/app/page-shell";

export default function SharingSettingsPage() {
  return (
    <PageShell title="Sharing" description="Manage outbound shares and pending invites.">
      <PhaseStub phase={6} />
    </PageShell>
  );
}
